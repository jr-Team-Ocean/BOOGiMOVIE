<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_PATH" value="C:/logs"/>
    <property name="LOG_FILE_NAME" value="stat_log"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level --- [%thread] %logger{35} : %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- 각 파일 최대 크기: 100MB를 넘으면 인덱스(i)를 올려 새 파일로 분할 -->
            <maxFileSize>100MB</maxFileSize>

            <!-- 보관 일수(또는 날짜 단위 히스토리): 30일치 유지 -->
            <maxHistory>30</maxHistory>

            <!-- 보관 파일들의 총 용량 상한: 2GB 넘으면 가장 오래된 것부터 삭제 -->
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

	<!-- 결제 로그 -->
    <logger name="STAT_LOGGER" level="INFO" additivity="true">
        <appender-ref ref="FILE"/>
    </logger>
    
    <!-- 검색어 로그 -->
    <logger name="SEARCH_LOGGER" level="INFO" additivity="true">
        <appender-ref ref="FILE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>